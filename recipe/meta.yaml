{% set name = "trata" %}
{% set version = "1.2.1" %}
{% set python_min = "3.9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/LLNL/trata/archive/refs/tags/{{ version }}.tar.gz
  sha256: 210c00006bee912855248eeb0eddef5f97185ba6d4248284b4fa05e67e634a47

build:
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  noarch: python
  entry_points:
    - trata = trata.trata:__init__

requirements:
  host:
    - python {{ python_min }}
    - pip
    - poetry
  run:
    - python >={{ python_min }}
    - scikit-learn
    - numpy
    - scipy
    - matplotlib-base

test:
  imports:
    - trata
  requires:
    - python {{ python_min }}
    - pip
  commands:
    - pip check

about:
  home: https://github.com/LLNL/trata
  summary: 'Sampling Methods Package'
  description: |
    trata offers a large number of general sampling strategies that can be used to explore parameter spaces or improve a model's predictive ability.
  license: MIT
  license_family: MIT
  license_file: LICENSE
  doc_url: https://trata.readthedocs.io/
  dev_url: https://github.com/trata/trata

extra:
  recipe-maintainers:
    - olson59
    - doutriaux1
